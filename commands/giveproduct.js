const discord = require('discord.js')

exports.run = async(client, message, args, level) => {
  
  
  
}

exports.conf = {
  enable: true,
  guildOnly: true,
  aliases: ["gp"],
  permlevel: "Staff`"
}